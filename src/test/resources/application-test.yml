spring:
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://localhost:1433;database=NOBANK;
    username: "sa"
    password: "Soleil.123"

  security:
    user:
      name: customer-data-api-user
      password: customer-data-api-password

  jpa:
    database-platform: org.hibernate.dialect.SQLServer2012Dialect
    database: SQL_SERVER
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      show-sql: true
      properties:
        hibernate:
          format_sql: true
          ddl-auto: update
          dialect: org.hibernate.dialect.SQLServer2012Dialect